import CartComponent from '@/components/cart/CartComponent';
import React from 'react';

const page = async () => {
  return (
    <div>
      <CartComponent />
    </div>
  );
};

export default page;
